# redis_client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build redis_client` to build the library.

## Running unit tests

Run `nx test redis_client` to execute the unit tests via [Vitest](https://vitest.dev/).
