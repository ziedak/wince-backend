# monitoring

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build monitoring` to build the library.

## Running unit tests

Run `nx test monitoring` to execute the unit tests via [Vitest](https://vitest.dev/).
