# kafka_client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build kafka_client` to build the library.

## Running unit tests

Run `nx test kafka_client` to execute the unit tests via [Vitest](https://vitest.dev/).
