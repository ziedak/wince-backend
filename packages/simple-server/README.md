# simple-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build simple-server` to build the library.

## Running unit tests

Run `nx test simple-server` to execute the unit tests via [Vitest](https://vitest.dev/).
