# postgre_client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build postgre_client` to build the library.

## Running unit tests

Run `nx test postgre_client` to execute the unit tests via [Vitest](https://vitest.dev/).
